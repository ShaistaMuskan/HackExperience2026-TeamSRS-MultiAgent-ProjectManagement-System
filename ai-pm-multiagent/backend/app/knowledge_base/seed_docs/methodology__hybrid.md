# Methodology Guide: Hybrid

Combines Waterfall-style phase gates (for governance, budget approval, and
compliance checkpoints) with Agile sprint execution inside each phase (for
delivery flexibility). Recommended as a safe default when a brief shows mixed
or ambiguous signals, or spans multiple work streams with different needs
(e.g., a regulated core platform plus a fast-moving customer-facing feature set).
